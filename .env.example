# -----------------------------------------------------------
# NOTE! Never push the .env file to a repository.
# This example documents the required environment variables.
# -----------------------------------------------------------

# API URL as seen from the browser (routed to the server container via Docker port)
VITE_API_URL=http://localhost:3000/api/v1

# Database
DB_PASSWORD=

# JWT
JWT_SECRET=
JWT_LIFE=

# TMDB
TMDB_API_KEY=